name: Render Auto-Deploy

on:
  push:
    branches:
      - main  # This triggers deployment when changes are pushed to the main branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'  # Specify the Python version you are using

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Deploy to Render
        run: |
          curl -X POST -H "Authorization: Bearer rnd_tauE42GaeLgseDxGMOmI22N0wmx0" \
          -H "Accept: application/json" \
          -H "Content-Type: application/json" \
          --data '{"serviceId": "srv-crukgvdumphs73ep3dr0"}' \
          https://api.render.com/v1/services/srv-crukgvdumphs73ep3dr0/deploys
